{
  "name": "slides",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "fusuma start",
    "build": "fusuma build",
    "deploy": "yarn build && fusuma deploy",
    "pdf": "fusuma pdf",
    "fmt": "prettier --write **/*.{js,json,css,md}",
    "precommit": "pretty-quick --staged"
  },
  "devDependencies": {
    "husky": "^0.14.3",
    "prettier": "^1.12.1",
    "pretty-quick": "^1.4.1"
  },
  "dependencies": {
    "@fortawesome/fontawesome-free": "^5.15.2",
    "fusuma": "^1.16.0"
  }
}
